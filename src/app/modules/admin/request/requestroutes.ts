import { Routes } from '@angular/router';
import { RequestComponent } from 'app/modules/admin/request/requestcomponent';

export default [
    {
        path     : '',
        component: RequestComponent,
    },
] as Routes;
